import React from 'react';
import DefaultContainer from '../../components/DefaultContainer';

const Home: React.FC = () => {

	return (
		<DefaultContainer>
			Hello, boilerplate!
		</DefaultContainer>
	);
};

export default Home;
